// check if an element exits in an array
// use case: when you need to verify if a value is present 

let numbers = [10,20,14,50,100]
console.log(numbers);

const target = numbers.includes(140)
console.log(target);


// merge multiple array
// use case: when you need to combine lists

let numbers = [10, 20, 14, 50, 100];
let numberstwo = [10, 20, 14, 50, 100];
let combined = numbers.concat(numberstwo)
console.log(combined);

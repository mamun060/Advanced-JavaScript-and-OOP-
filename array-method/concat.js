// The cancat() method is used to merge two or more arrays. this method does not change the exiting arrays, but instead returns a new array

// for example 
const a = ["a","b","c"];
const b = ["d","e","f"];
const c = ["g","h","i"];

const finalArray = a.concat(b, c);

console.table(finalArray);
// console.log(finalArray);
// mutable = value can be changed directly
// If you modify the original data in place, it is mutable.

const user = {
    name: "Md Al Mamun"
}

user.name = "Raju Ahmed"

console.log(user.name);

// this is mutable because his changes the same memory.
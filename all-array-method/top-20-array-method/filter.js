// create a new array with elements that pass a condition 
// use case: when you need to filter elements based on a criterion 

let numbers = [10,20,14,50,100]
console.log(numbers);
let evens = numbers.filter((num)=> num / 2 === 50);
console.log(evens);

